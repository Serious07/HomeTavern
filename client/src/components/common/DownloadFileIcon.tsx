import React from 'react';

interface DownloadFileIconProps {
  className?: string;
}

/**
 * Иконка экспорта/скачивания .txt-файла.
 *
 * - Лист бумаги (stroke-only) со срезанным верхним правым углом и линией сгиба.
 *   Контур НЕ замкнут в правом нижнем углу — там «окно»/разрыв под стрелку:
 *   правый край обрывается НАД стрелкой, нижний — СЛЕВА от неё, поэтому линии
 *   контура не пересекаются со стрелкой.
 * - По центру листа — БОЛЬШАЯ заглавная «T» (две stroke-линии: планка + стойка),
 *   читается чётко на размере 20×20.
 * - В правом нижнем углу — массивная стрелка-бейдж (ствол + широкая наконечник),
 *   залита цветом и обведена широкой линией; рисуется поверх «окна» листа.
 *
 * Единый цвет — currentColor. Файл и «T» — stroke-only; стрелка — filled-бейдж.
 *
 * Геометрия (viewBox 0 0 32 32):
 *   - лист:  левый край (5,25)→(5,3), верх →(20,3), срез →(24,7), правый край ↓ к (24,12);
 *            нижний край (5,25)→(18,25). Разрыв между x18/x24 и y12/25 = окно под стрелку.
 *   - сгиб:  (20,3)→(20,7)→(24,7)
 *   - «T»:   планка M8.5 9 H19.5, стойка M14 9 V19
 *   - стрелка: ствол (23..27, 14..18), наконечник (19,18)-(31,18)-(25,26)
 */
const DownloadFileIcon: React.FC<DownloadFileIconProps> = ({ className = 'w-5 h-5' }) => (
  <svg className={className} fill="none" stroke="currentColor" viewBox="0 0 32 32" aria-hidden="true">
    {/* Лист: левый край, верх, срезанный угол, правый край (обрывается над стрелкой);
        нижний край обрывается слева от стрелки — окно/разрыв в правом нижнем углу */}
    <path
      strokeLinecap="round"
      strokeLinejoin="round"
      strokeWidth={1.8}
      d="M5 25H18 M5 25V3h15l4 4v5 M20 3v4h4"
    />

    {/* Большая заглавная «T» (stroke-only, currentColor) — по центру/чуть выше центра листа */}
    <path
      strokeLinecap="round"
      strokeLinejoin="round"
      strokeWidth={2}
      d="M8.5 9h11 M14 9v10"
    />

    {/* Массивная стрелка-бейдж в правом нижнем углу поверх «окна» листа.
        Заполнена цветом + обведена широкой линией; контур листа в этой зоне не доходит до неё. */}
    <path
      d="M23 14h4v4h4l-6 8-6-8h4z"
      fill="currentColor"
      stroke="currentColor"
      strokeWidth={1.9}
      strokeLinecap="round"
      strokeLinejoin="round"
    />
  </svg>
);

export default DownloadFileIcon;
